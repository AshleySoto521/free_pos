engine-strict=true
# pnpm 11 corre un chequeo de deps antes de cada script y lanza un install
# que falla por el build script bloqueado de core-js. Lo desactivamos: las
# dependencias se instalan manualmente con `pnpm install` cuando cambian.
verify-deps-before-run=false
