// SPA: sin renderizado en servidor (Tauri sirve archivos estáticos y todo
// se resuelve en el cliente). Aplica a toda la app por estar en la raíz.
export const ssr = false;
export const prerender = false;
